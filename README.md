# Create a new folder
mkdir bsu-supply-order
cd bsu-supply-order

# Initialize git
git init

# Save your HTML file as index.html in this folder

# Add and commit
git add index.html
git commit -m "Add supply order form"

# Create repo on GitHub, then push
git remote add origin https://github.com/yourusername/bsu-supply-order.git
git branch -M main
git push -u origin main
